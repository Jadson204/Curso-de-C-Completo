#include <stdio.h>
#include <stdlib.h>

int main () {

  float x = 1.0;

  printf("Tamanho em memoria de um float: %d bytes\n", sizeof(x));

  printf("Tamanho dem memoria de um int: %d bytes\n", sizeof(int));

  return 0;
}